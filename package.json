{
  "name": "ai-powered-pr-description-generator",
  "version": "1.0.0",
  "description": "GitHub Action automatically generates descriptive summaries for pull requests using AI, enhancing clarity and context for reviewers.",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=dist/bundle.js --platform=node"
  },
  "author": "Rock Chen",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
